# Copy this file to .env and fill in your values
# .env is gitignored and will not be committed
# This setting is ONLY used for local docker development

# AWS SSO profile name (run 'aws configure list-profiles' to see available profiles)
AWS_PROFILE=your-sso-profile-name
